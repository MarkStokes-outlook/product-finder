# Project Memory

This file captures long-lived, repo-specific conventions that agents should respect.

## Stable Conventions
- Layout: `src/` layout Python package (`src/product_finder/`), installed editable via `pip install -e ".[dev]"`.
- Dependencies: minimal by design — PyYAML + requests only. Do not add heavyweight frameworks.
- CLI-first: argparse subcommands in `cli.py`; orchestration lives in `runner.py` so it stays testable.
- Config: single YAML file (`config.yaml`, gitignored; `config.example.yaml` is the committed reference).

## Domain Concepts
- `project` — a group of wanted items (e.g. Coachhouse tools); `item` — one wanted product with search terms and prices.
- `normal_price` — operator's estimate of market value; `target_deal_price` — at/under this = deal; `max_price` — hard filter.
- `grade` — rule-based condition class: A / B / C / spares/repair / unknown (`grading.py`). Spares keywords always win.
- `warning flags` — false-bargain signals (faulty, untested, no charger…) in `scoring.py`.
- `deal_score` — 0–100 heuristic combining margin, target, grade, priority, flags.
- Sources: eBay automated via official Browse API (needs free dev keys in config); Gumtree and Facebook are manual-assisted link generators only (ToS compliance — never scrape).
- Dedup: listings unique on `(source, external_id)`; alerts fire once per `(listing, item)` match per channel (`alerts_sent`).

- Sources are class-based behind a formal contract (`sources/base.py::Source`: name, is_automated, search, manual_links). Registry built per-run from config (`sources.build_registry`). Two generic config-driven types under `sources.extra` in YAML: `rss` (automated, needs £ price in entry text, throttled 3s between fetches) and `links` (manual URL templates with {term}/{max_price}/{postcode}/{radius}). New sites are usually YAML-only. Note: HUKD per-search RSS no longer exists (only /rss/hot); Reddit search.rss works but rate-limits hard.

## Testing & Quality
- Run: `pytest` (or `.venv/bin/pytest`). 22 tests cover grading, scoring, dedup, config loading.
- Good enough = pragmatic MVP: correctness of scoring/dedup matters; polish does not.

## Agent Usage Notes
- 2026-07-02: MVP built from `prompts/1-initial-prompt.md` (Claude, developer role). Full package, tests, README usage docs. Not yet committed.
- 2026-07-02: Static HTML report added from `prompts/02-web-ui.prompt` (`alerts/html.py`, `report-html` command). HTML path derives from `report_path` with `.html` suffix — no new config. Written alongside Markdown on every run.
- 2026-07-02: Full local Flask web UI added (`web/app.py` + templates, `web` command, port 8765). ARCHITECTURE SHIFT: projects/items now live in SQLite (terms/exclude_terms/sources JSON columns, archived flags) — the DB is the source of truth after first seed; YAML keeps settings only. YAML auto-seeds an empty DB (or one with only term-less items); `import-config` command / "Import from YAML" button force-merges. Runner and CLI load projects via `db.load_project_configs`. Flask dep added.
- Do not add web UI, accounts, or plugin architecture — explicitly out of scope.
- Compliance is a hard constraint: no login bypass, no CAPTCHA/bot-protection evasion, official APIs or manual links only.
- 2026-07-02: Populated `sources.extra` in config.example.yaml with real, curl-verified UK endpoints (no code, config-only): `hardwareswapuk` (rss, r/HardwareSwapUK — confirmed query is server-honoured; NB it's PC/electronics hardware, not power tools), `vinted` (links, confirmed term reflected in response body), `johnpyeauctions` (links — domain moved from johnpye.co.uk to johnpyeauctions.co.uk, updated), `preloved` (links), `cexwebuy` (links, graded secondhand electronics/tools). Rejected after testing: HotUKDeals per-search RSS (404, dead), Shpock (query stripped on redirect), the-saleroom.com (AWS WAF challenge blocks verification), easyliveauction.com (404 on tested URL pattern), MusicMagpie (403). Preloved/CeX are JS SPAs — curl couldn't fully confirm server-side query honouring (identical payload size for real vs nonsense term), included on well-established param names but worth a manual click-check if listings look off.
- 2026-07-02: Added 3 demo projects to config.example.yaml, each scoped via per-item `sources:` to the endpoints that actually suit the domain: `gaming-pc-upgrade` (GPU/monitor/CPU bundle → hardwareswapuk + cexwebuy; live-tested, "RTX 3080" returns real on-topic listings unlike the mismatched "hardwareswapuk = tools" assumption caught earlier), `workshop-garden-kit` (generator/pressure washer → johnpyeauctions + preloved), `home-office-refresh` (chair/desk → vinted + preloved). `tests/test_config.py::test_load_example_config` was pinned to exactly 1 project — loosened to assert by slug rather than count/index so the example config can keep growing without breaking the test.
- 2026-07-02/03: `config.example.yaml` is a template only — actual runtime state lives in `config.yaml` (gitignored) + the SQLite DB. Adding sources/projects to the example doesn't touch either; must also update `config.yaml` and run `import-config`. The running `web` process caches `AppConfig` in closures at startup too, so it needs a restart after any config.yaml change — editing the file alone does nothing to a live process.
- 2026-07-03: Fixed 3 issues found using the demo projects for real: (1) Web UI "Edit project" only renames — items live on a separate `/items?project_id=` page; added a direct link between them (`project_form.html`) since there was no discoverable path from one to the other. (2) `hardwareswapuk` "CPU + Motherboard Bundle" item was matching full prebuilt PCs (sellers describe whole builds using component keywords) — replaced with two single-component items (`CPU`, `Motherboard`) and added a shared set of full-system exclude_terms (desktop pc, gaming pc, prebuilt, complete system, tower pc, itx pc, etc.) across all three Gaming PC items. (3) Reddit search keeps posts searchable indefinitely, so `hardwareswapuk` matches could be years-old and long-sold — added `max_age_days` to `ExtraSourceConfig` (rss-only), date parsing in `sources/rss.py` (`_parse_date`, handles RFC822 pubDate + Atom published/updated), entries without a parseable date are kept rather than dropped. Set to 90 days for hardwareswapuk. Tests added in `test_sources.py` (parse + filtering) and `test_config.py`-style validation in `test_sources.py`.
- 2026-07-03: KNOWN GAP surfaced by the above: `db.record_match` only inserts/updates — nothing ever prunes a match once recorded, even if the source stops returning that listing later (sold/delisted/filtered out by a config change like max_age_days). Manually purged the 5 pre-existing hardwareswapuk matches/17 listings that predated the age filter as a one-off cleanup; this is not a general fix. If stale matches become a recurring complaint, the real fix is a pruning strategy in the runner/db layer (e.g. mark absent-from-latest-run matches stale after N runs) — not yet designed or discussed with the user.
- 2026-07-03: Decoupled search execution from the web process. `cmd_watch`/`python -m product_finder watch` already existed (interval_minutes loop) but was undocumented for background use and unsafe to run alongside `web` — `db.connect()` had no WAL/busy_timeout, so concurrent writers would risk "database is locked". Added `PRAGMA journal_mode=WAL` + `busy_timeout=10000` to `db.connect()`. The web UI's "Run search now" button called `runner.run_once()` inline in the Flask request (blocking 30s+, coupling scheduled runs to the UI being open) — changed `/run` to `subprocess.Popen([...,"run-once"])` instead (fire-and-forget, confirmed request now returns in ~6ms vs previously blocking). `create_app(cfg, config_path)` now takes the config path so the spawned subprocess reloads the same file. Verified live: ran `watch` and a manually-triggered `run-once` concurrently against the same DB, no lock errors. README has a new "Running on a schedule" section (`nohup ... watch &` + pointer to launchd/systemd for reboot survival — not installed, just documented as the next step if wanted). Test added: `test_web.py::test_run_now_spawns_background_process_not_inline` (mocks Popen, asserts `runner.run_once` is never called inline).
- 2026-07-03: `watch` (background scheduler, interval_minutes=60) and `web` are both currently running as detached background processes on this machine (started via `nohup ... &`, not a system service — won't survive reboot/logout). PIDs will differ next session; check with `lsof -nP -iTCP:8765` (web) or `ps aux | grep "product_finder watch"`.
- 2026-07-03: Added a Sources page (`/sources`) so eBay API keys and per-source enable/disable can be set without editing YAML. Design decision: source *definitions* (url/type/label, `sources.extra` in YAML) are never duplicated into the DB — only two things are overridable, in a new `source_settings` table: `enabled` and eBay `app_id`/`cert_id`/`env`. Read path is `db.effective_sources_config()`/`effective_config()` (YAML as base, DB overlaid via `row_value or yaml_value`, so a blank DB value/no row just falls through to YAML — no seed step needed, no clobber risk). This means adding a new source to config.yaml shows up on the Sources page immediately with zero import/sync action, unlike projects/items. Wired into every cfg-consuming entry point: `runner.run_once`, `cli.py` (`cmd_run_once`, `cmd_watch` — re-resolved every loop iteration so a live toggle takes effect within one cycle, `cmd_report*`), and `web/app.py` (per-request via `_effective_cfg(cfg)`, cached on Flask's `g` — NOT computed once at app startup, since that would need a server restart to see toggles). Caught and fixed a related bug while doing this: `dashboard.html` read `cfg.sources.enabled_names()` directly from the stale startup-time closure `cfg` instead of the effective one — switched it to the already-correct `known_sources` context var.
- 2026-07-03: Verified live end-to-end on a throwaway instance (port 8766, separate from the user's own running `web`/`watch` processes — did not touch those): toggled `hardwareswapuk` off and set eBay sandbox test keys via HTTP, then confirmed via a completely separate fresh Python process (`db.effective_config`) that both changes were visible — proves overrides are genuinely global DB state, not per-process. Reverted both test changes afterward so the real DB wasn't left dirty. Note for future sessions: don't kill the user's manually-started `web`/`watch` processes without being asked again — they explicitly wanted to control those themselves from their own terminal (see conversation ~2026-07-03).
- 2026-07-03: Real eBay Production credentials (App ID + Cert ID) saved via the Sources page — confirmed working with a live Browse API call (50 real listings for "track saw"). eBay is now a second fully-automated source alongside hardwareswapuk. Notability: the eBay dev portal's "User Tokens (eBay Sign-in)" tab (OAuth/Auth'n'Auth user flow, needs a redirect URL/RuName) is a red herring for this app — `ebay.py` only ever uses the `client_credentials` grant (app-level token), so Dev ID and the whole redirect-URL/sign-in flow are irrelevant; only App ID + Cert ID from the "Application Keys" page are needed.
- 2026-07-03: Removed the manual "Run search now" button and its `/run` route/subprocess-spawn entirely (now genuinely dead — search only ever happens via `watch`/`run-once`, never inside the web process). `create_app`/`cmd_web` reverted to not needing a `config_path` param (it existed solely to support that subprocess call). Added live dashboard refresh instead: `_dashboard_data()`/`_reports_mtime()` helpers in `web/app.py`; new `_dashboard_live.html` partial (fragment, no `base.html`) shared by both the full `/` page (`{% include %}`, so first load isn't AJAX-dependent) and a new `/dashboard/live` fragment-only route; a cheap `/api/status` JSON endpoint exposes just the reports' mtime (rewritten on every completed run regardless of new matches, so it's a reliable "did a run just happen" signal without new DB columns). Vanilla JS in `dashboard.html` polls `/api/status` every 15s and only fetches/swaps `/dashboard/live` when the mtime actually changes — no full page reloads, deliberately no external JS library/CDN (stays consistent with "localhost only, no cloud"). Verified live: touched the report files to simulate a completed run, confirmed `/api/status`'s mtime changed and `/dashboard/live` returned fresh content with no `<html>`/`<nav>` wrapper.
- 2026-07-03: Real eBay Production credentials confirmed live (see eBay-portal conversation same day) — 50 real listings returned for "track saw" via the live Browse API. eBay is now automated alongside hardwareswapuk.
- 2026-07-03: Removed the static HTML report entirely (`alerts/html.py`, `report-html` CLI command, `tests/test_html_report.py`, the "HTML report" link) — replaced by a live **per-project dashboard page**, which is what the user actually wanted from "the html report" (grouped-by-item match tables + manual links), just live instead of a regenerated static file. New: `db.project_detail_matches(conn, project_id)` (same `_MATCH_SELECT` pattern as `report_rows`, scoped + ordered by item name); `web/app.py::_project_detail_data()` groups matches by `item_id` in Python (fetches ALL active items via `db.list_items(..., include_archived=False)` — including items with zero matches yet, unlike the old static report which only ever showed items that already had a match; this is a deliberate improvement, not just parity). Routes: `/projects/<id>` (full page) and `/projects/<id>/live` (fragment) — same poll-and-swap pattern as the main dashboard, reusing the same global `/api/status` mtime signal (a completed run is treated as "maybe this project changed too" rather than tracking per-project freshness — simpler, good enough for a personal-scale tool). Project name links (Projects page, Dashboard project cards) now go to this page instead of straight to Items. Markdown report (`alerts/markdown.py`, `report` command) is untouched — only the HTML one was superseded, since it's still useful as a portable/plaintext artifact the live UI can't replace.
- 2026-07-03: Four-part change requested and confirmed by the user in one go (removed Markdown report, project-level sources, fold Items/Listings into the project page, dashboard redesign). All shipped together, 88 tests passing:
  1. **Markdown report removed** — the user decided it was no longer needed now the live pages cover the same ground. Deleted `alerts/markdown.py`, `report` CLI command, `AlertsConfig.markdown_report`, `AppConfig.report_path`, `/reports/<kind>` route, `_reports_mtime()`. The live-polling "did a run just happen" signal (previously the report file's mtime) is now `db.latest_activity()` = `MAX(listings.last_seen)` — every listing touched in a cycle gets `last_seen` bumped regardless of whether it's new, so this still changes on any cycle that fetched at least one listing, with no new DB column needed. `/api/status` now returns `{"last_activity": ...}`; templates/JS renamed `reports_mtime`/`data-mtime` → `last_activity`/`data-activity` accordingly.
  2. **Project-level source restriction** — new nullable `projects.sources` TEXT column (JSON list, same `NULL = no restriction` convention as `items.sources`), migrated via the existing `_MIGRATIONS` list. `ProjectConfig.sources` added to `config.py`, validated in `load_config` alongside item-level sources. `runner.item_sources(item, cfg, project=None)` now takes an optional project and narrows enabled ∩ project.sources ∩ item.sources (each level can only narrow, never widen) — updated at all three call sites (`run_once`, `collect_manual_links`, `web/app.py`'s `/manual` route). Project create/edit forms grew source checkboxes (`project_form.html`, mirrors the item form's pattern); `db.create_project`/`update_project` take an optional `sources` list now — **note this is a signature change**: `update_project` always overwrites `sources` (defaults to `None`/unrestricted if not passed), so any future caller must pass the current value through if it doesn't want to accidentally clear a restriction.
  3. **Items & Listings folded into the project detail page** — the user's reasoning was that both only make sense in the context of a project, so standalone browsing across projects added no value. Deleted `items.html`/`listings.html` templates and their `/items`/`/listings` list routes entirely. Item add/edit is now always project-scoped: `item_form.html` lost its project `<select>` dropdown (replaced with a hidden `project_id` field + a fixed "Project: X" label), and `item_new`/`item_edit`/`item_archive`/`item_delete` all redirect to `project_detail` instead of the now-gone items list. Listing filters (source/grade/flagged/sort/item) moved onto the project page via query-string params — `db.query_matches(..., project_id=...)` replaced the old unfiltered `project_detail_matches` (deleted), and the live-poll JS on the project page now appends `window.location.search` to the fragment URL so a filtered view keeps auto-refreshing within the same filter. Nav bar lost the "Items"/"Listings" links.
  4. **Dashboard redesign** — explicit brief from the user: make it obvious "here's a deal you should grab now" rather than a flat list. Added a hero strip (`.hero-grid`/`.deal-card` CSS in `base.html`) showing the top 3 non-flagged matches by score as cards (title, big price, saving %, "under target", project/item/source) — always genuinely the best regardless of any listing filters active below. Project cards on the dashboard now show a live "top pick" preview line (`db.project_top_picks()`, a `ROW_NUMBER() OVER (PARTITION BY project_id ORDER BY deal_score DESC)` window query, one best match per active project) or an italic "Still watching — no matches yet" idle state if the project has no matches. The project detail page got the same single-card hero treatment for that project's best deal specifically. Remaining matches beyond the hero three are demoted to a plain "More matches" table; warnings table unchanged structurally.
  - Full test rewrite in `test_web.py` to match: mtime-based polling tests → activity-based; global `/items`/`/listings` tests → project-scoped equivalents + explicit 404 checks that those routes are gone; new coverage for project-source restriction (web form → DB → `runner.item_sources` narrowing), dashboard hero/idle states. `test_config.py` gained project-level `sources:` validation tests; `config.example.yaml`'s Coachhouse Tools project now demonstrates the exact "no CeX for power tools" scenario the user described, with a project-level `sources:` list excluding the electronics-only endpoints.
