# Project Memory

This file captures long-lived, repo-specific conventions that agents should respect.

## Stable Conventions
- Layout: `src/` layout Python package (`src/product_finder/`), installed editable via `pip install -e ".[dev]"`.
- Dependencies: minimal by design — PyYAML + requests only. Do not add heavyweight frameworks.
- CLI-first: argparse subcommands in `cli.py`; orchestration lives in `runner.py` so it stays testable.
- Config: single YAML file (`config.yaml`, gitignored; `config.example.yaml` is the committed reference).

## Domain Concepts
- `project` — a group of wanted items (e.g. Coachhouse tools); `item` — one wanted product with search terms and prices.
- `normal_price` — operator's estimate of market value; `target_deal_price` — at/under this = deal; `max_price` — hard filter.
- `grade` — rule-based condition class: A / B / C / spares/repair / unknown (`grading.py`). Spares keywords always win.
- `warning flags` — false-bargain signals (faulty, untested, no charger…) in `scoring.py`.
- `deal_score` — 0–100 heuristic combining margin, target, grade, priority, flags.
- Sources: eBay automated via official Browse API (needs free dev keys in config); Gumtree and Facebook are manual-assisted link generators only (ToS compliance — never scrape).
- Dedup: listings unique on `(source, external_id)`; alerts fire once per `(listing, item)` match per channel (`alerts_sent`).

- Sources are class-based behind a formal contract (`sources/base.py::Source`: name, is_automated, search, manual_links). Registry built per-run from config (`sources.build_registry`). Two generic config-driven types under `sources.extra` in YAML: `rss` (automated, needs £ price in entry text, throttled 3s between fetches) and `links` (manual URL templates with {term}/{max_price}/{postcode}/{radius}). New sites are usually YAML-only. Note: HUKD per-search RSS no longer exists (only /rss/hot); Reddit search.rss works but rate-limits hard.

## Testing & Quality
- Run: `pytest` (or `.venv/bin/pytest`). 22 tests cover grading, scoring, dedup, config loading.
- Good enough = pragmatic MVP: correctness of scoring/dedup matters; polish does not.

## Agent Usage Notes
- 2026-07-02: MVP built from `prompts/1-initial-prompt.md` (Claude, developer role). Full package, tests, README usage docs. Not yet committed.
- 2026-07-02: Static HTML report added from `prompts/02-web-ui.prompt` (`alerts/html.py`, `report-html` command). HTML path derives from `report_path` with `.html` suffix — no new config. Written alongside Markdown on every run.
- 2026-07-02: Full local Flask web UI added (`web/app.py` + templates, `web` command, port 8765). ARCHITECTURE SHIFT: projects/items now live in SQLite (terms/exclude_terms/sources JSON columns, archived flags) — the DB is the source of truth after first seed; YAML keeps settings only. YAML auto-seeds an empty DB (or one with only term-less items); `import-config` command / "Import from YAML" button force-merges. Runner and CLI load projects via `db.load_project_configs`. Flask dep added.
- Do not add web UI, accounts, or plugin architecture — explicitly out of scope.
- Compliance is a hard constraint: no login bypass, no CAPTCHA/bot-protection evasion, official APIs or manual links only.
- 2026-07-02: Populated `sources.extra` in config.example.yaml with real, curl-verified UK endpoints (no code, config-only): `hardwareswapuk` (rss, r/HardwareSwapUK — confirmed query is server-honoured; NB it's PC/electronics hardware, not power tools), `vinted` (links, confirmed term reflected in response body), `johnpyeauctions` (links — domain moved from johnpye.co.uk to johnpyeauctions.co.uk, updated), `preloved` (links), `cexwebuy` (links, graded secondhand electronics/tools). Rejected after testing: HotUKDeals per-search RSS (404, dead), Shpock (query stripped on redirect), the-saleroom.com (AWS WAF challenge blocks verification), easyliveauction.com (404 on tested URL pattern), MusicMagpie (403). Preloved/CeX are JS SPAs — curl couldn't fully confirm server-side query honouring (identical payload size for real vs nonsense term), included on well-established param names but worth a manual click-check if listings look off.
- 2026-07-02: Added 3 demo projects to config.example.yaml, each scoped via per-item `sources:` to the endpoints that actually suit the domain: `gaming-pc-upgrade` (GPU/monitor/CPU bundle → hardwareswapuk + cexwebuy; live-tested, "RTX 3080" returns real on-topic listings unlike the mismatched "hardwareswapuk = tools" assumption caught earlier), `workshop-garden-kit` (generator/pressure washer → johnpyeauctions + preloved), `home-office-refresh` (chair/desk → vinted + preloved). `tests/test_config.py::test_load_example_config` was pinned to exactly 1 project — loosened to assert by slug rather than count/index so the example config can keep growing without breaking the test.
- 2026-07-02/03: `config.example.yaml` is a template only — actual runtime state lives in `config.yaml` (gitignored) + the SQLite DB. Adding sources/projects to the example doesn't touch either; must also update `config.yaml` and run `import-config`. The running `web` process caches `AppConfig` in closures at startup too, so it needs a restart after any config.yaml change — editing the file alone does nothing to a live process.
- 2026-07-03: Fixed 3 issues found using the demo projects for real: (1) Web UI "Edit project" only renames — items live on a separate `/items?project_id=` page; added a direct link between them (`project_form.html`) since there was no discoverable path from one to the other. (2) `hardwareswapuk` "CPU + Motherboard Bundle" item was matching full prebuilt PCs (sellers describe whole builds using component keywords) — replaced with two single-component items (`CPU`, `Motherboard`) and added a shared set of full-system exclude_terms (desktop pc, gaming pc, prebuilt, complete system, tower pc, itx pc, etc.) across all three Gaming PC items. (3) Reddit search keeps posts searchable indefinitely, so `hardwareswapuk` matches could be years-old and long-sold — added `max_age_days` to `ExtraSourceConfig` (rss-only), date parsing in `sources/rss.py` (`_parse_date`, handles RFC822 pubDate + Atom published/updated), entries without a parseable date are kept rather than dropped. Set to 90 days for hardwareswapuk. Tests added in `test_sources.py` (parse + filtering) and `test_config.py`-style validation in `test_sources.py`.
- 2026-07-03: KNOWN GAP surfaced by the above: `db.record_match` only inserts/updates — nothing ever prunes a match once recorded, even if the source stops returning that listing later (sold/delisted/filtered out by a config change like max_age_days). Manually purged the 5 pre-existing hardwareswapuk matches/17 listings that predated the age filter as a one-off cleanup; this is not a general fix. If stale matches become a recurring complaint, the real fix is a pruning strategy in the runner/db layer (e.g. mark absent-from-latest-run matches stale after N runs) — not yet designed or discussed with the user.
- 2026-07-03: Decoupled search execution from the web process. `cmd_watch`/`python -m product_finder watch` already existed (interval_minutes loop) but was undocumented for background use and unsafe to run alongside `web` — `db.connect()` had no WAL/busy_timeout, so concurrent writers would risk "database is locked". Added `PRAGMA journal_mode=WAL` + `busy_timeout=10000` to `db.connect()`. The web UI's "Run search now" button called `runner.run_once()` inline in the Flask request (blocking 30s+, coupling scheduled runs to the UI being open) — changed `/run` to `subprocess.Popen([...,"run-once"])` instead (fire-and-forget, confirmed request now returns in ~6ms vs previously blocking). `create_app(cfg, config_path)` now takes the config path so the spawned subprocess reloads the same file. Verified live: ran `watch` and a manually-triggered `run-once` concurrently against the same DB, no lock errors. README has a new "Running on a schedule" section (`nohup ... watch &` + pointer to launchd/systemd for reboot survival — not installed, just documented as the next step if wanted). Test added: `test_web.py::test_run_now_spawns_background_process_not_inline` (mocks Popen, asserts `runner.run_once` is never called inline).
- 2026-07-03: `watch` (background scheduler, interval_minutes=60) and `web` are both currently running as detached background processes on this machine (started via `nohup ... &`, not a system service — won't survive reboot/logout). PIDs will differ next session; check with `lsof -nP -iTCP:8765` (web) or `ps aux | grep "product_finder watch"`.
- 2026-07-03: Added a Sources page (`/sources`) so eBay API keys and per-source enable/disable can be set without editing YAML. Design decision: source *definitions* (url/type/label, `sources.extra` in YAML) are never duplicated into the DB — only two things are overridable, in a new `source_settings` table: `enabled` and eBay `app_id`/`cert_id`/`env`. Read path is `db.effective_sources_config()`/`effective_config()` (YAML as base, DB overlaid via `row_value or yaml_value`, so a blank DB value/no row just falls through to YAML — no seed step needed, no clobber risk). This means adding a new source to config.yaml shows up on the Sources page immediately with zero import/sync action, unlike projects/items. Wired into every cfg-consuming entry point: `runner.run_once`, `cli.py` (`cmd_run_once`, `cmd_watch` — re-resolved every loop iteration so a live toggle takes effect within one cycle, `cmd_report*`), and `web/app.py` (per-request via `_effective_cfg(cfg)`, cached on Flask's `g` — NOT computed once at app startup, since that would need a server restart to see toggles). Caught and fixed a related bug while doing this: `dashboard.html` read `cfg.sources.enabled_names()` directly from the stale startup-time closure `cfg` instead of the effective one — switched it to the already-correct `known_sources` context var.
- 2026-07-03: Verified live end-to-end on a throwaway instance (port 8766, separate from the user's own running `web`/`watch` processes — did not touch those): toggled `hardwareswapuk` off and set eBay sandbox test keys via HTTP, then confirmed via a completely separate fresh Python process (`db.effective_config`) that both changes were visible — proves overrides are genuinely global DB state, not per-process. Reverted both test changes afterward so the real DB wasn't left dirty. Note for future sessions: don't kill the user's manually-started `web`/`watch` processes without being asked again — they explicitly wanted to control those themselves from their own terminal (see conversation ~2026-07-03).
- 2026-07-03: Real eBay Production credentials (App ID + Cert ID) saved via the Sources page — confirmed working with a live Browse API call (50 real listings for "track saw"). eBay is now a second fully-automated source alongside hardwareswapuk. Notability: the eBay dev portal's "User Tokens (eBay Sign-in)" tab (OAuth/Auth'n'Auth user flow, needs a redirect URL/RuName) is a red herring for this app — `ebay.py` only ever uses the `client_credentials` grant (app-level token), so Dev ID and the whole redirect-URL/sign-in flow are irrelevant; only App ID + Cert ID from the "Application Keys" page are needed.
- 2026-07-03: Removed the manual "Run search now" button and its `/run` route/subprocess-spawn entirely (now genuinely dead — search only ever happens via `watch`/`run-once`, never inside the web process). `create_app`/`cmd_web` reverted to not needing a `config_path` param (it existed solely to support that subprocess call). Added live dashboard refresh instead: `_dashboard_data()`/`_reports_mtime()` helpers in `web/app.py`; new `_dashboard_live.html` partial (fragment, no `base.html`) shared by both the full `/` page (`{% include %}`, so first load isn't AJAX-dependent) and a new `/dashboard/live` fragment-only route; a cheap `/api/status` JSON endpoint exposes just the reports' mtime (rewritten on every completed run regardless of new matches, so it's a reliable "did a run just happen" signal without new DB columns). Vanilla JS in `dashboard.html` polls `/api/status` every 15s and only fetches/swaps `/dashboard/live` when the mtime actually changes — no full page reloads, deliberately no external JS library/CDN (stays consistent with "localhost only, no cloud"). Verified live: touched the report files to simulate a completed run, confirmed `/api/status`'s mtime changed and `/dashboard/live` returned fresh content with no `<html>`/`<nav>` wrapper.
- 2026-07-03: Real eBay Production credentials confirmed live (see eBay-portal conversation same day) — 50 real listings returned for "track saw" via the live Browse API. eBay is now automated alongside hardwareswapuk.
- 2026-07-03: Removed the static HTML report entirely (`alerts/html.py`, `report-html` CLI command, `tests/test_html_report.py`, the "HTML report" link) — replaced by a live **per-project dashboard page**, which is what the user actually wanted from "the html report" (grouped-by-item match tables + manual links), just live instead of a regenerated static file. New: `db.project_detail_matches(conn, project_id)` (same `_MATCH_SELECT` pattern as `report_rows`, scoped + ordered by item name); `web/app.py::_project_detail_data()` groups matches by `item_id` in Python (fetches ALL active items via `db.list_items(..., include_archived=False)` — including items with zero matches yet, unlike the old static report which only ever showed items that already had a match; this is a deliberate improvement, not just parity). Routes: `/projects/<id>` (full page) and `/projects/<id>/live` (fragment) — same poll-and-swap pattern as the main dashboard, reusing the same global `/api/status` mtime signal (a completed run is treated as "maybe this project changed too" rather than tracking per-project freshness — simpler, good enough for a personal-scale tool). Project name links (Projects page, Dashboard project cards) now go to this page instead of straight to Items. Markdown report (`alerts/markdown.py`, `report` command) is untouched — only the HTML one was superseded, since it's still useful as a portable/plaintext artifact the live UI can't replace.
- 2026-07-03: Four-part change requested and confirmed by the user in one go (removed Markdown report, project-level sources, fold Items/Listings into the project page, dashboard redesign). All shipped together, 88 tests passing:
  1. **Markdown report removed** — the user decided it was no longer needed now the live pages cover the same ground. Deleted `alerts/markdown.py`, `report` CLI command, `AlertsConfig.markdown_report`, `AppConfig.report_path`, `/reports/<kind>` route, `_reports_mtime()`. The live-polling "did a run just happen" signal (previously the report file's mtime) is now `db.latest_activity()` = `MAX(listings.last_seen)` — every listing touched in a cycle gets `last_seen` bumped regardless of whether it's new, so this still changes on any cycle that fetched at least one listing, with no new DB column needed. `/api/status` now returns `{"last_activity": ...}`; templates/JS renamed `reports_mtime`/`data-mtime` → `last_activity`/`data-activity` accordingly.
  2. **Project-level source restriction** — new nullable `projects.sources` TEXT column (JSON list, same `NULL = no restriction` convention as `items.sources`), migrated via the existing `_MIGRATIONS` list. `ProjectConfig.sources` added to `config.py`, validated in `load_config` alongside item-level sources. `runner.item_sources(item, cfg, project=None)` now takes an optional project and narrows enabled ∩ project.sources ∩ item.sources (each level can only narrow, never widen) — updated at all three call sites (`run_once`, `collect_manual_links`, `web/app.py`'s `/manual` route). Project create/edit forms grew source checkboxes (`project_form.html`, mirrors the item form's pattern); `db.create_project`/`update_project` take an optional `sources` list now — **note this is a signature change**: `update_project` always overwrites `sources` (defaults to `None`/unrestricted if not passed), so any future caller must pass the current value through if it doesn't want to accidentally clear a restriction.
  3. **Items & Listings folded into the project detail page** — the user's reasoning was that both only make sense in the context of a project, so standalone browsing across projects added no value. Deleted `items.html`/`listings.html` templates and their `/items`/`/listings` list routes entirely. Item add/edit is now always project-scoped: `item_form.html` lost its project `<select>` dropdown (replaced with a hidden `project_id` field + a fixed "Project: X" label), and `item_new`/`item_edit`/`item_archive`/`item_delete` all redirect to `project_detail` instead of the now-gone items list. Listing filters (source/grade/flagged/sort/item) moved onto the project page via query-string params — `db.query_matches(..., project_id=...)` replaced the old unfiltered `project_detail_matches` (deleted), and the live-poll JS on the project page now appends `window.location.search` to the fragment URL so a filtered view keeps auto-refreshing within the same filter. Nav bar lost the "Items"/"Listings" links.
  4. **Dashboard redesign** — explicit brief from the user: make it obvious "here's a deal you should grab now" rather than a flat list. Added a hero strip (`.hero-grid`/`.deal-card` CSS in `base.html`) showing the top 3 non-flagged matches by score as cards (title, big price, saving %, "under target", project/item/source) — always genuinely the best regardless of any listing filters active below. Project cards on the dashboard now show a live "top pick" preview line (`db.project_top_picks()`, a `ROW_NUMBER() OVER (PARTITION BY project_id ORDER BY deal_score DESC)` window query, one best match per active project) or an italic "Still watching — no matches yet" idle state if the project has no matches. The project detail page got the same single-card hero treatment for that project's best deal specifically. Remaining matches beyond the hero three are demoted to a plain "More matches" table; warnings table unchanged structurally.
  - Full test rewrite in `test_web.py` to match: mtime-based polling tests → activity-based; global `/items`/`/listings` tests → project-scoped equivalents + explicit 404 checks that those routes are gone; new coverage for project-source restriction (web form → DB → `runner.item_sources` narrowing), dashboard hero/idle states. `test_config.py` gained project-level `sources:` validation tests; `config.example.yaml`'s Coachhouse Tools project now demonstrates the exact "no CeX for power tools" scenario the user described, with a project-level `sources:` list excluding the electronics-only endpoints.
- 2026-07-04: UI overhaul ("make it fucking awesome" brief). Design intent: instant "oh shit, grab that" dopamine on page load. Key decisions: (1) **Always-dark theme** — deliberately dropped the `prefers-color-scheme` light variant from base.html; the glow/contrast language (ambient radial pools, green hero glow) only works dark. Restore point is git history if Mark ever wants light back. (2) All existing CSS class contracts kept (`deal-card`, `project-pick`, `score`, `badge`, etc. — tests pin several); new components added alongside: `.spotlight` (full-width #1 deal card, pulsing green border), `.stat-strip` (hot deals / new-in-24h / live matches / projects, count-up on load), `.ring` (SVG score ring, r=15.9155 so circumference=100 and the score IS the dasharray), `.target-bar` (price position between normal and target), `.chip-new` (first_seen < 24h), `.badge.auction` with live JS countdown from `data-ends` (red + blinking under 1h), `.trend` chip (products.price_trend_pct — first UI surfacing of the trend data, previously backend-only). (3) Warnings demoted to a collapsed `<details class="warnings">` (auto-`open` only when there are no clean deals) — bad news never competes with good. (4) `_ui.html` is the new shared macro home (spotlight/deal_card/score_ring/target_bar) used by both dashboard and project-detail live fragments — macros REQUIRE full `_MATCH_SELECT` rows (top_picks rows lack the new columns). (5) `_MATCH_SELECT` gained l.last_seen/end_time/bid_count/buying_options + pr.price_trend_pct/confidence; new `db.dashboard_stats()`; new Jinja filters `timeago`/`is_new` in app.py. (6) Live-swap JS now calls `pfEnhance()` (paginate+countup+countdown) instead of bare `pfPaginate()`, adds a `.swap-in` fade and flashes the nav "watching" dot green for 8s after a refresh lands. 304 tests passing (was 298; 6 new). Verified live on a throwaway 8766 instance with seeded scratch DB — Mark's own web/watch processes not touched; **his web process needs a manual restart to pick up the new UI**.
- 2026-07-04 (second pass, same day): Mark's feedback — first pass was "crypto dashboard", he wants a **product discovery experience** (eBay/HotUKDeals/Steam feed, not trading terminal). Product-card UX pass shipped: (1) **`listings.image_url`** (nullable TEXT, `_MIGRATIONS`) — eBay Browse API images verified live first: `thumbnailImages[0].imageUrl` is counterintuitively the LARGE render (~1200-1600px), `image.imageUrl` the 225px one; we store the big one. RSS gets best-effort `media:thumbnail`/image-enclosure via `_entry_image()` in rss.py. `upsert_listing` refreshes image on rescan but via `COALESCE(?, image_url)` so a source that stops sending one never blanks a stored image; existing listings gain images within ~1 watch cycle after restart. `additionalImages` (gallery, 1-14 per listing) deliberately NOT stored — single image is enough for cards, revisit only if a gallery view is ever wanted. (2) Spotlight/deal cards rebuilt as product cards in `_ui.html`: image-led (spotlight = image left 22rem, cards = 16:10 image top), shop-style pricing (`price_block`: big price + struck-through was-price + Steam-style `-53%` `.pct-off` pill), explicit CTA on spotlight only (`cta_label`: "View on eBay ↗" vs "Open listing ↗" — runner cards stay whole-card links, standard feed UX), score ring demoted to a small overlay in the image corner (secondary, per Mark). 📦 CSS placeholder (`.thumb::before`) shows when image missing/broken (`onerror="this.remove()"`); external imgs get `loading="lazy" referrerpolicy="no-referrer"`. Thumbnails added to match tables (`.row-thumb`) and project-card pick previews (`.pick-thumb`, `project_top_picks` now selects l.image_url). (3) De-terminal-ed the theme: killed ambient radial glows + spotlight pulse animation + uppercase tracking-heavy h2s (now sentence case); warmer neutral dark palette. Kept per Mark: countdowns, trend chips, warnings foldaway, stat strip (quieter), live heartbeat, swap-in fade. 308 tests passing (4 new: rss image parse, upsert image refresh/never-blank, spotlight img + placeholder paths). Still uncommitted at Mark's request — he wants to review the product-card pass first.
- 2026-07-04 (deal_score recalibration, SHIPPED after analysis + Mark's approval): decomposed the saturation on the real DB first. Two stacked causes: (1) formula headroom — additive max 111 vs the 100 clamp (40 base + 36 margin cap + 15 target + 10 grade + 10 priority); 43% of clean matches sat at exactly 111 raw, and the bonuses were correlated (target bonus 100% redundant among 100-scorers, grade A = 73% of all clean matches). (2) Dominant data cause was **accessory pollution, not inflated normals**: only 5.4% of clean matches resolve to a catalogue product, and the cheap 100-scorers were accessories/spares (£1.17 blade screw, £4 hose adaptors vs £500-600 item normals → 90% "below normal" → margin cap). Shipped changes in scoring.py, all thresholds as named constants at the top of the module per Mark: `margin_term()` inverted-U (linear ×0.6 to +30 at 50% below, plateau to 70%, decay to +10 at 85%, floor −10; **verified** rows — i.e. product-matched, trusted reference price — keep the plateau forever); `is_price_implausible()` gate (unverified below 12% of normal) adds a `price implausible for item` warning flag in `evaluate()` (which excludes it from the spotlight via the existing flagged filter, forces under_target=False, and via the ordinary flags/false-bargain penalties sinks these to ~0); baseline 40→35, target +15→+10, grade A +10→+5 / B +2. **Priority removed from deal_score entirely** — Mark's architectural call: score = "how objectively good is the deal", priority = "how much do I care", to be used later in ranking/spotlight/personalised opportunity scoring, NOT in the score. Verified on the real DB: 0 scores of 100 (was 2,851), clean max 80, median 60, ~30% ≥70; 1,583 matches now implausible-flagged (median score 0); top-8 became genuine deals (Aeron £120, real mitre saws ~half price). Explicitly an INTERIM fix: root cause is catalogue identity/classification (complete product vs accessory/spare/bundle) — that stays the roadmap direction. Note: `HOT_DEAL_SCORE=70` (web) / `deal_score >= 70` (db stats) now means "top ~30% of clean" — semantics still reasonable, deliberately not retuned in this pass. 316 tests passing (8 new in test_scoring.py). Mark's watch process must be restarted to pick this up; stored scores rewrite naturally as listings re-match each cycle.
- 2026-07-04 (afternoon, post-recalibration — details in docs/implementation-notes/, filenames now YYYY-MM-DD-HHMM-slug per Mark's convention for chronological ordering; write one such notes file after EVERY completed piece of work, for his external architect agent): (1) Scoring consistency (6030fbc): under_target AND deal_score's target bonus now withheld in lockstep for all three ambiguous-price categories (live auction / multi-item / implausible-price); flag literals promoted to scoring constants FLAG_LIVE_AUCTION/FLAG_MULTI_ITEM; project_top_picks() now clean-only (same predicate as query_matches(flagged=False)), all-flagged projects show the idle card. (2) Ended listings (ccebbb1): db._NOT_ENDED read-time predicate (lexical UTC compare vs SQLite 'now') on query_matches/project_top_picks/project_summaries/dashboard_stats — ended listings vanish at end time, not a cycle later; rows never deleted; auction_watch deliberately untouched (it must poll past end time). Known gap still open: stale listings with NO end_time (sold RSS items) linger — pruning undesigned. (3) Mark rewrote the roadmap himself (692ba63): new strategic phase = MARKET COVERAGE + product understanding, six knowledge layers (Catalogue/Pricing/Coverage/Identity/Trust/Recommendations), "every marketplace is a connector, not a special case". I then committed roadmap clarifications he dictated (c7e6f1e): Ollama extraction moved to shipped-foundation, next catalogue objective = extraction quality + richer product understanding; connector classes codified (automated vs manual-assisted, ToS compliance a hard constraint, Facebook/Gumtree stay manual-assisted). (4) Connector framework v1 (04d5af1): Source.capabilities() abstract → every connector declares class + mandatory compliance prose (tested non-empty); get_item_details() on base contract, runner enrichment now capability-gated (isinstance(EbaySource) special case REMOVED — test fakes declare capabilities, no more mock.patch of runner.EbaySource); eBay's is_automated() stays credential-dependent = operational readiness vs declared class; new source_runs table (self-prunes >30 days on write) + db.record_source_run()/source_health() fed by run_once; Sources page has Class/Compliance/Health columns; sources.build_all() = every connector for display, build_registry() = operational set. 332 tests. Deliberately NOT done: per-connector scheduling, new marketplaces (each candidate starts with a ToS check), seller-identity capability field (no reader yet). Mark's watch+web processes still needed restarts as of ~17:00 (watch: recalibration+consistency+framework; web: read-path + Sources UI) — never restart them yourself, tell him. Next queued task (agreed this morning, still pending): fuzzy cross-marketplace identity grouping with confirm/dismiss review UI mirroring product_suggestions' pending/approved/dismissed pattern — real duplicates visible on dashboard (two identical VIEWEDGE monitors).
- 2026-07-05 (01:25, f36addd): Approve-with-model-correction SHIPPED (Mark's real case: Metabo suggestion recorded as "613216380" = order number; real model "KGS 216 M" — Metabo SKUs are 9-digit codes, this is a whole CLASS: structured brand/mpn fields often carry article numbers). approve_suggestion(model=...) optional override: product gets corrected model; RAW sighted string kept as extra match term (sellers quote article numbers — still earns matches); suggestion row keeps raw model as don't-ask-again dedup key (correcting it would let the same raw sighting reopen); later suggestion of the corrected model CONVERGES onto same product via create_product case-insensitive guard (tested). /catalogue "part no.?"-flagged rows get inline "real model name?" input in the Approve form. 409 tests (3 new). Possible future: auto-suggest the real model from evidence titles (titles containing the article number usually also contain "KGS 216 M") — not built.
- 2026-07-05 (01:10, e35dfe5): Suggestion triage SHIPPED (Mark hit the queue: "holy crap, there's a lot of products to check"). db.triage_pending_suggestions: evidence = item's own matched listings whose titles mention the model (word-boundary; ≥2 required, no evidence no verdict); verdicts strong (avg price ≥40% of item normal AND ≤25% accessory-worded → approve as bucket) / accessory (≥50% accessory-worded OR <25% price → dismiss as bucket) / unclear / brand-only. /catalogue suggestions section REWRITTEN verdict-grouped (was item-grouped) with select-all per bucket; verdicts are proposals only, existing confidence-based auto_approve_threshold untouched (upgrading it to require strong verdict = obvious next step once Mark trusts buckets). Live queue: 1,621 → 48 strong / 127 accessory / 1,043 unclear (mostly "no listings mention model" — from get_item_details structured fields; resolves as listings accumulate) / 403 brand-only; 0.6s at page load (precompute in watch cycle if 10× growth). CAVEAT told to Mark: accessory bucket can contain class-2 (real product, accessory-only mentions: VC2012L, Leica NA724) — skim before bucket-dismiss; real fix stays the listing-level accessory gate. 406 tests (6 new).
- 2026-07-05 (00:35, c480edd): Knowledge-only products SHIPPED (Mark: "I want older CPUs and products in the catalogue, but I don't want them matched against that project" → concept: IDENTIFICATION ≠ ENDORSEMENT). `products.wanted` (migration, default 1): wanted=0 = still matched by catalogue.match (identification, price observations keep accumulating, listings stay out of suggestion churn) but never alerted (runner gate: `product is None or product.wanted`) and never on deal surfaces — shared `db._WANTED` predicate ("m.product_id IS NULL OR pr.wanted=1") applied at READ TIME to query_matches/project_top_picks/dashboard_stats/project_summaries, so toggling is instant + fully reversible, no rescan. Suspects UI now has the two correct verdicts side by side: "Archive selected" (true accessories) vs "Keep as knowledge-only" (real-but-unwanted, e.g. old CPU tiers); knowledge-only products stop being accused by find_suspect_products. Item form: per-product toggle + badge. Archived stays the harder state (stops matching entirely) — do NOT "fix" class-2 suspects (accessory LISTINGS matching parent products, "bags for VC2000L") with this flag; that needs the listing-level accessory gate, still TOP catalogue follow-up. If products ever become item-independent (roadmap), wanted moves to the item↔product link. Live DB migrated (284/284 wanted=1, dashboards unchanged until toggled). 401 tests (6 new). Mark still to review the 28 suspects on /catalogue; watch/web still stopped at his request.
- 2026-07-05 (midnight, cb544d1): Suspect products SHIPPED after Mark's audit question + his architectural stance ("we cannot rely on human approved product approvals — too many, we'll get it wrong" → recorded as standing direction: EVIDENCE GATES, HUMANS ARBITRATE SMALL LISTS). Audit found: 284 products post-tidy, 62% never matched anything, ~65 accessory-priced, HARIBO ×2, part numbers as models (Wagner 517xxx = spray tips, Festool 204308 = dust bags, Titan 730-401 = repair kit, StarTech HDMI cables under Monitor). Shipped: catalogue.ACCESSORY_KEYWORDS/accessory_title_share/looks_like_part_number (part-number shape NEVER accuses alone — Wagner uses bare article numbers for real sprayers); db.find_suspect_products (≥2 matches required — no evidence no accusation; price-ratio <25% of item normal OR ≥50% accessory-titled matches); /catalogue "Suspect products" section with evidence prose + bulk archive (reversible; record_match refreshes product_id so archived products' matches un-verify within one rescan). Live run: 28 flagged in THREE classes — (1) ~10 true accessories → archive; (2) REAL products with accessory-polluted match streams ("bags FOR Makita VC2000L" verifies against parent product — do NOT archive, needs LISTING-level accessory gate before catalogue.match, the top follow-up); (3) real-but-old/cheap (Intel SR-code CPUs, DW088CG) accused by price-ratio because item normal_price spans tiers — Mark's call. 395 tests. Gotcha found: db.connect() treats 'file:...?mode=ro' URI as literal path and CREATES a junk dir+DB (cleaned up; unguarded). Mark has NOT yet reviewed/archived the 28 — /catalogue is where. Follow-ups in priority order: listing-level accessory gate → suggestion-time evidence (skip evidence-poor in bulk approve, make auto-approve safe) → old CPU tier question.
- 2026-07-04 (night, 49e07b9): Catalogue tidy-up SHIPPED (Mark's brief: "creating Products from the suggestions, removing duplicates, generally tidying the catalogue up"). Measured first on real DB: 1,613 pending suggestions/18 items reviewable only via item-edit forms (Mark hand-approved ~288 then stopped); 425 brand-only; 36 case-split groups; 6 exact-dup products; "Herman Miller NOT FOUND" ×45. Shipped: global /catalogue review page (grouped by item, per-item select-all, sticky bulk bar, shared endpoints via validated relative-`next`); bulk-approve SKIPS brand-only server-side (badged in UI, individual approve still allowed — bare-brand products defeat model-level pricing); case-insensitive suggestion merge at record time (COLLATE NOCASE, first-seen casing adopted — BRAND_ALIASES now display-only); placeholder models (NOT FOUND/None/see description/etc.) collapse to brand-only; create_product heals case-insensitive dups (guard at single insert path — UNIQUE migration would fail on already-dup'd DBs); merge_products/dedupe_products (full ownership transfer + match_terms union + COALESCE prices + used-price recompute via newly-factored _recompute_used_price); `catalogue-tidy` CLI (idempotent; FIRST caller renormalize_pending_suggestions ever had). Snapshot dry-run: 1,615→1,573 pending, 6 dups folded. **LIVE TIDY RUN later same night with Mark's explicit permission to stop his processes: watch+web (PIDs 48021/47604, started 22:31-22:32 on pre-catalogue code) killed cleanly, tidy ran: 1,620→1,578 pending / 42 merged / 6 dup products folded / Herman Miller unified ×57 / 0 dup groups left. Both processes left STOPPED — Mark said he doesn't need them running; next start picks up all new code (no restart-pending debt remains).** 387 tests (15 new). NOT done deliberately: fuzzy product reconciliation (DW088K vs DW088K-XJ — needs propose/confirm pattern like product_suggestions; natural next step), brand-only rejection at source, auto-deleting junk products (HARIBO stays, Mark's call via existing UI).
- 2026-07-04 (late evening, e19afb0): Coverage metrics SHIPPED (roadmap "coverage should become measurable", sequenced BEFORE the next connector so connector picks are steered by data — agreed with Mark this evening). `db.source_coverage()`: pure read-time aggregation, no schema change — per source: listings total/live, new 24h/7d, stale (end_time IS NULL AND last_seen >48h old, i.e. "source stopped returning it, nothing explains why" — distinct from max_age_days which handles old-but-still-returned; this metric SIZES the undesigned pruning problem), hidden duplicates (is_primary_sighting=0, covers identity v1+v2), catalogue match rate (product_id matches / all matches), price obs 30d (keyed on observations' own source column, so contributors without listings rows still appear). Rendered as a quiet second table on /sources, automated-class connectors only. NOT measurable yet (documented in docstring): suggestion yield per marketplace (product_suggestions.source = discovery mechanism, not marketplace) and enrichment success rate (only the attempt is recorded). Real-DB smoke (VACUUM INTO snapshot, Mark's processes untouched): **eBay 4,978 listings, 7% catalogue match, 107 dups hidden, 77 price obs; hardwareswapuk 8 listings, 0% match** — the 7% number quantifies the accessory-pollution/catalogue bottleneck; re-check it after any extraction-quality work. 372 tests (10 new, tests/test_coverage.py). Mark's web process needs a restart to see the Coverage table (read-only feature — watch does NOT need one). Next per this evening's agreement: first new automated connector, candidate ToS checks first (Reverb = only roadmap candidate with a confirmed official API).
- 2026-07-04 (evening, 1da65da): Fuzzy duplicate grouping v1 SHIPPED (design-first: docs/design/2026-07-04-fuzzy-duplicate-grouping.md, approved by Mark with all three open-question recommendations accepted). New duplicates.py (pure, stdlib difflib) + listing_duplicates table + scan in run_once + "Possible duplicates" review UI on project page + dashboard note. KEY DESIGN FACTS: (1) the reference VIEWEDGE pair is SAME-marketplace (two eBay item IDs, same seller location EN6***) — fuzzy grouping is not only cross-marketplace; (2) identical title ≠ duplicate — same-source pairs REQUIRE seller evidence (matching non-empty location OR identical image URL) else they're different sellers selling the same model (measured: 716 exact-title live pairs, 167 same-location); (3) suppression reuses is_primary_sighting so zero read-path changes; resolve_identity's promotion branch now defers to confirmed duplicate decisions (_is_hidden_duplicate guard); (4) pairs not groups — confirmed groups emerge as stars around the kept listing; (5) decided pairs never re-proposed (UNIQUE(listing_a,listing_b) = don't-ask-again memory), everything undoable via Decided-pairs foldaway. Real-data smoke (scratch DB copy): 620 initial pending pairs (410 in project 1) → web renders top 30 by confidence with true total in heading; VIEWEDGE queues at 89%. Known gaps by design: hidden listing stays hidden if kept one ends (Undo covers); no perceptual image hashing; relister who hides location slips through. 362 tests. Mark's watch AND web both need restarts to activate (never restart them yourself).
- 2026-07-04 (third pass): Calm-down UX pass after Mark's "sensory overload" feedback. Two findings first: (1) **Images weren't broken in code** — his `watch` process (started 09:07) predated the image-parsing commit (09:50), so 0/4,424 listings had image_url despite the new code returning 50/50 images in a live eBay check; fix is restarting watch, images backfill in one cycle. (2) **deal_score saturates on real data: 2,850/4,223 clean matches score exactly 100, 95% score ≥70** — "hot" is currently meaningless as a differentiator; scoring recalibration flagged as follow-up, NOT done. UI changes: score rings deleted entirely (macro + CSS, score stays as a plain muted number in tables); `tr.excellent` green row-wash removed (warning-red rows kept); stat tiles → one quiet single-line strip (no colours, no count-up — pfCountUp deleted); chip-new restyled from green gradient shout to quiet outline; runner cards stripped to image/title/price+pct-pill/meta (condition only shown when below A; no under-target/trend/grade chips); under-target badge + NEW chip removed from tables (Saving column / Seen column cover them); spotlight is now the single special element (green border, CTA, under-target badge, trend chip, target bar). Flame emoji gone from headings. Also fixed a hollow test: test_dashboard_with_data's `b"under target"` assertion was matching a **CSS comment** in base.html, not rendered UI — now asserts the real spotlight badge text. 308 tests passing. Committed together with a rendered-body "noise census" verification on the throwaway 8766 instance.
